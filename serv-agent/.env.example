# Batpilot SERV agent env — NEVER commit this file (gitignored).
# WALLET_PRIVATE_KEY  — OpenServ platform identity (auto-created by provision()).
# RHC_KEY             — agent's OWN Robinhood mainnet key. Fund with ~$20 USDG
#                       + 0.0005 ETH gas. Blast radius capped; deployer key stays out.
# OPENSERV_USER_API_KEY — only needed for `npx @openserv-labs/client deploy`.
WALLET_PRIVATE_KEY=
RHC_KEY=0xYOUR_AGENT_KEY_HERE
