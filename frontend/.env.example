# Batpilot frontend needs no env vars: chains, vaults, feeds and public RPCs
# are baked into src/config.ts (no secrets — reads are permissionless).
# Only create a local .env to override during development; it is gitignored.
